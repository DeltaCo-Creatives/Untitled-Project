# frontend

Placeholder. This will be a React + Vite app (deployed to Vercel with Root Directory `frontend`), scaffolded once the backend's Drive + Gemini loop is proven out. Not built yet — do not add React/Vite tooling here until that's explicitly requested.
